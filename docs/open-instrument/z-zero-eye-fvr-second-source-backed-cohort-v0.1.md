# ZË-RO EYE FVR Second Source-Backed Cohort v0.1

Date: 2026-08-30

Status: IMPLEMENTED_RESEARCH_SURFACE.

The bounded EYE research cohort is implemented in the current generic catalog,
`/api/research/fvr`, `/chat` recurrence card, and provider-disabled live smoke.
Its recurrence meaning remains `research_hypothesis`; this status does not
promote the observation to statistics, historical origin, cognacy, or
production linguistic truth.

## Purpose

This lane adds the second real source-backed Seven-Voice Functional Recurrence
research cohort to Open Instrument.

The first live research cohort is WATER.

The second cohort is:

`EYE`

The lane reuses the existing deterministic recurrence engine, fail-closed cohort
evidence admission, generic research catalog, `/api/research/fvr` endpoint and
Cross-Language Recurrence card.

It does not alter the single-word Analyze V1 truth model.

## Source-backed comparison forms

### English

Source:

Oxford Advanced Learner's Dictionary — `eye`.

Source URL:

`https://www.oxfordlearnersdictionaries.com/us/definition/english/eye_1`

Declared row:

- surface: `EYE`
- comparison: `EYE`
- mode: `orthography`
- authority: `source_orthography`
- Seven-Voice path: `E-Y-E`

### Albanian

Source:

IE-CoR Cognate Set 211 — `eye`.

Source URL:

`https://iecor.clld.org/cognatesets/211`

Declared Standard Albanian row:

- surface: `SY`
- comparison: `SY`
- mode: `orthography`
- authority: `source_orthography`
- Seven-Voice path: `Y`

Neither row requires transliteration or ZË-RO functional normalization.

## Deterministic recurrence result

Within the two declared source-admitted comparison forms:

`EYE -> E-Y-E`

`SY -> Y`

the shared canonical intersection is:

`Y`

Therefore:

**EYE cohort -> shared functional nucleus Y**

This recurrence observation is factual only within those declared comparison
forms.

## Interpretation boundary

The claim that recurrent `Y` carries or motivates an EYE / vision-related
functional principle remains:

`research_hypothesis`

This lane does not claim:

- phonetic identity;
- that English `eye` contains Albanian phonetic `/y/`;
- historical origin;
- historical transmission;
- cognacy as an FVR conclusion;
- borrowing;
- candidate truth;
- language superiority;
- universality.

Historical information present in source material remains source context only.

User decision posture remains:

`user_decides`

Project posture remains:

`no_single_winner`

## UI mode separation

The generic recurrence card previously contained WATER-specific explanatory
copy.

That copy is not valid for an ordinary orthographic cohort such as EYE.

The card therefore distinguishes:

- cohorts containing explicit `z_zero_functional_normalization`, which retain
  explicit normalization-separation disclosure;
- cohorts using other declared modes, which receive generic comparison-mode
  separation disclosure.

For WATER, the UI must continue to show the explicit:

`WATER -> UOTER`

comparison identity dynamically from admitted research data.

For EYE, WATER-specific text must not appear.

In every case FVR research remains separate from Analyze V1.

## Expected live research surface

Request:

`/api/research/fvr?concept=eye`

Expected:

- status: `available`
- concept id: `EYE`
- comparison forms: `EYE`, `SY`
- shared functional nucleus: `Y`
- recurrence observation truth:
  `fact_within_declared_comparison_forms`
- functional meaning truth:
  `research_hypothesis`

Unknown concepts continue to fail closed as:

`404 not_available`

## Protected architecture

This lane does not modify:

- `/api/analyze-v1`;
- `functionalVoiceNormalization.v0.1.ts`;
- `sevenVoiceFunctionalRecurrence.v0_1.ts`;
- `sevenVoiceFunctionalRecurrenceCohortEvidence.v0_1.ts`;
- provider execution;
- security remediation;
- JO parked work.

## Research ordering

No recurrence statistics are authorized by this milestone.

The recurrence rule remains:

**Evidence admission precedes recurrence statistics.**

The governing doctrine remains:

**Logic discovers. Evidence validates or contextualizes. Promotion remains strict.**
