# Open Instrument — DI Canon-Lock Lifecycle Transition v0.1

## Status

DI-only governance transition.

DI changes from:

- `runtime_verified`

to:

- `canon_locked`.

DA remains:

- `canon_locked`.

## Admitted scope

DI is canon locked only under:

- `bounded_functional_lexical_projection`.

The admitted scope is derived from the existing machine-readable
`authorizationScope`.

No duplicate scope field is introduced.

## Machine-readable admission

Canon-lock admission remains owned by:

- `src/shared/canonicalOperatorCanonLockAdmission.v0_1.ts`.

The explicit admission set now contains:

- `DA`;
- `DI`.

Admission still fails closed unless every existing requirement passes.

DI admission resolves to:

- admitted:
  `true`;
- admitted scope:
  `bounded_functional_lexical_projection`;
- rollback lifecycle:
  `runtime_verified`;
- reasons:
  empty.

## DI identity and proof coverage

DI retains:

- operator ID:
  `DI`;
- embryo:
  `DI`;
- language:
  `sq`;
- source ID:
  `reviewed.external.di.knowledge.candidate.v0_1`;
- bounded lexical function:
  `know / knowledge functional motivation`;
- positive proof words:
  - `di`;
  - `study`;
  - `studim`;
- negative controls:
  - `da`;
  - `dam`;
  - `damage`;
  - `mode`;
  - `xyz`;
  - `dij`;
  - `dije`;
  - `dit`.

## Evidence-operation and carrier boundary

DI reviewed evidence remains limited to:

- operations:
  - `exact`;
  - `y_to_i`;
- selected carrier:
  - `di`.

The transition does not admit reviewed evidence for:

- `dij`;
- `dije`;
- `dit`.

Those forms may retain token and diagnostic visibility while reviewed DI
citation evidence remains absent.

## Source-authority boundary

Unresolved direct DPEWA/FGJSH locator or archive work remains documented.

It continues to block stronger source-authority or historical-authority
framing.

It does not block the admitted bounded functional scope.

Canon lock does not convert the reviewed functional source into historical
origin evidence.

## Runtime continuity

The transition does not change:

- reviewed source rows;
- citations;
- functional readiness;
- runtime authorization;
- production membership;
- runtime projection;
- operation authorization;
- carrier authorization;
- RootMap construction;
- API output;
- UI behavior;
- candidate ranking;
- proof words;
- negative controls;
- runtime evidence text;
- live-smoke case generation.

The live smoke already accepts both runtime-mature lifecycle states:

- `runtime_verified`;
- `canon_locked`.

## Historical records

The readiness reassessment remains a historical pre-transition record.

Earlier DA-only and DI-readiness documents are not rewritten to pretend that
their original lane had already performed this transition.

Current machine truth is owned by the canonical profile and admission
evaluator.

## Rollback

The governance-only rollback target is:

- `runtime_verified`.

A governance-only rollback does not require changes to source rows, citations,
authorization, production membership, runtime projection, RootMap, API, or UI.

A finding that invalidates runtime safety requires a separate broader review.

## Claim boundaries

DI canon lock does not establish:

- historical origin;
- historical transmission;
- borrowing direction;
- earliest attestation;
- linguistic ownership;
- candidate truth;
- winner status;
- language superiority;
- scientific evidence;
- publication evidence.

The user-decision posture remains:

- `user_decides`.
