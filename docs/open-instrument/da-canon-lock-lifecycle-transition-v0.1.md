# Open Instrument — DA Canon-Lock Lifecycle Transition v0.1

## Status

DA-only governance transition.

DA changes from:

- `runtime_verified`

to:

- `canon_locked`.

DI remains:

- `runtime_verified`.

## Admitted scope

DA is canon locked only under:

- `bounded_functional_lexical_projection`.

The admitted scope is derived from the existing machine-readable
`authorizationScope`.

No duplicate canon-lock scope field is added.

## Machine-readable admission owner

Canon-lock admission is evaluated by:

- `src/shared/canonicalOperatorCanonLockAdmission.v0_1.ts`.

The evaluator fails closed unless the operator preserves:

- explicit operator admission;
- runtime-mature lifecycle state;
- reviewed functional evidence;
- functional readiness;
- machine authorization;
- matching authorization scope;
- explicit production membership;
- non-null runtime projection;
- source identity;
- embryo identity;
- positive proof words;
- negative controls;
- non-overlapping proof sets;
- historical-origin boundary;
- winner boundary;
- language-superiority boundary;
- user-decision posture.

## DA identity

- operator ID: `DA`;
- embryo: `DA`;
- language: `sq`;
- source ID:
  `reviewed.external.gheg-da.damage.candidate.v0_1`;
- bounded lexical function:
  `split / divide functional motivation`;
- positive proof words:
  - `da`;
  - `dam`;
  - `damage`;
- negative controls:
  - `study`;
  - `xyz`.

## Runtime-mature lifecycle states

The shared canonical and live-smoke contracts recognize:

- `runtime_verified`;
- `canon_locked`.

`canon_locked` is stronger governance status.

It does not bypass runtime readiness, authorization, membership, projection,
proof-word, citation-isolation, or claim-boundary requirements.

Earlier lifecycle states and `deprecated` remain invalid for production
live-smoke execution.

## Rollback

The governance-only rollback target is:

- `runtime_verified`.

A governance-only rollback does not require changing:

- source rows;
- citations;
- readiness;
- authorization;
- production membership;
- runtime projection;
- RootMap;
- API;
- UI.

A finding that invalidates runtime safety requires a separate broader rollback
review.

## DI boundary

DI is not admitted by this transition.

DI remains:

- `runtime_verified`.

Its unresolved direct DPEWA/FGJSH authority remains relevant to stronger
source-authority or historical-authority claims.

## Claim boundaries

Canon lock does not establish:

- historical origin;
- historical transmission;
- language ownership;
- earliest attestation;
- candidate truth;
- winner status;
- language superiority.

The user-decision posture remains:

- `user_decides`.

## Scope boundary

This transition does not change:

- source-row evidence;
- citation content;
- functional readiness;
- runtime authorization;
- production membership;
- runtime projection;
- RootMap;
- API output;
- UI behavior;
- candidate ranking;
- proof words;
- negative controls;
- runtime evidence content.
