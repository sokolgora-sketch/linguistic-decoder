# Open Instrument — AT Canon-Lock Lifecycle Transition v0.1

## Status

AT-only governance transition.

AT changes from:

- `runtime_verified`

to:

- `canon_locked`.

DA and DI remain:

- `canon_locked`.

## Admitted scope

AT is canon locked only under:

- `bounded_functional_lexical_projection`.

The admitted scope remains derived from the existing machine-readable
`authorizationScope`.

AT discovery remains independently constrained by:

- `bounded_targets`.

Canon lock does not change AT discovery breadth.

## Machine-readable admission

Canon-lock admission remains owned by:

- `src/shared/canonicalOperatorCanonLockAdmission.v0_1.ts`.

The explicit admission set now contains:

- `DA`;
- `DI`;
- `AT`.

AT admission resolves to:

- admitted:
  `true`;
- admitted scope:
  `bounded_functional_lexical_projection`;
- rollback lifecycle:
  `runtime_verified`;
- reasons:
  empty.

## AT identity and boundary

AT retains:

- operator ID:
  `AT`;
- embryo:
  `AT`;
- language:
  `sq`;
- source ID:
  `reviewed.external.albanian-at.father.candidate.v0_1`;
- bounded lexical function:
  `father functional motivation`;
- reviewed evidence:
  `reviewed_functional`;
- discovery scope:
  `bounded_targets`;
- positive proof:
  `father`.

Bare `at` remains a negative homograph control.

Canon lock does not create:

- a global `at = father` interpretation;
- reviewed father truth for bare `at`;
- broad AT discovery across arbitrary words.

Unrelated inputs including `diet`, `data`, `random`, `later`, and `water`
remain blocked from isolated reviewed AT discovery.

Legacy structural carriers `atë`, `ati`, and `pater` remain structurally
available without becoming reviewed canonical father evidence.

## Runtime continuity

The transition does not change:

- reviewed source rows;
- citation metadata;
- functional readiness;
- runtime authorization;
- production membership;
- runtime projection;
- evidence operation policy;
- carrier authorization;
- RootMap construction;
- API output;
- UI behavior;
- candidate ranking;
- proof words;
- negative controls;
- runtime evidence text;
- discovery scope;
- live-smoke case generation.

## Historical records

Earlier AT source-admission documentation remains historical.

Its statement that AT did not enter `canon_locked` in that earlier repair
remains correct for that earlier lane.

The present dedicated transition supersedes only current machine lifecycle and
admission state.

## Rollback

The governance-only rollback target is:

- `runtime_verified`.

Rollback does not automatically change `bounded_targets` or any underlying
source, authorization, membership, projection, RootMap, API, or UI truth.

## Revision, supersession, revocation and deprecation

Future AT governance revisions must identify affected profile fields, source
identity, proof words, negative controls, discovery scope, compatibility,
runtime impact, regression requirements, and rollback procedure.

Supersession preserves historical traceability.

Revocation or deprecation must fail closed rather than silently widening AT
discovery or claim authority.

## Claim boundaries

AT canon lock does not establish:

- historical origin;
- historical transmission;
- borrowing direction;
- earliest attestation;
- linguistic ownership;
- candidate truth;
- winner status;
- language superiority;
- scientific evidence;
- publication evidence.

The user-decision posture remains:

- `user_decides`.
