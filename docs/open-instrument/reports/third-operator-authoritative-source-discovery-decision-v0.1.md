# Third-Operator Authoritative-Source Discovery Decision v0.1

Status: DECISION_ONLY.

Project lane:

- Open Instrument / ZË-RO.

## Result

The current result is:

- `NO_CURRENT_REVIEWED_THIRD_OPERATOR`.

The next research lane is:

- `AUTHORITATIVE_THIRD_OPERATOR_SOURCE_DISCOVERY`.

No third operator is selected by this decision.

## Current reusable pipeline owners

The current canonical profiles are:

- DA;
- DI.

The current reviewed production source rows are:

- `reviewed.external.gheg-da.damage.candidate.v0_1`;
- `reviewed.external.di.knowledge.candidate.v0_1`.

The current machine-authorized reviewed source IDs are the same two rows.

The current reviewed operation and carrier policies are owned only by:

- DA;
- DI.

The current explicit canon-lock admission set contains only:

- DA;
- DI.

Both operators are currently:

- `canon_locked`.

Their admitted scope remains:

- `bounded_functional_lexical_projection`.

## Claim-boundary continuity

Each current reviewed source row preserves:

- `originClaim: false`;
- `historicalTransmissionClaim: false`;
- `winnerClaim: false`;
- `languageSuperiorityClaim: false`;
- `candidateTruthClaim: false`;
- `publicationEvidenceClaim: false`;
- `scientificEvidenceClaim: false`;
- `userDecisionPosture: user_decides`.

No claim boundary changes in this lane.

## Third-source-row result

There is currently no reviewed external production source row outside the
existing DA and DI profile sources.

Therefore:

- no third source row can be ranked for production readiness;
- no third source ID can be machine authorized;
- no third source ID can enter production membership;
- no third runtime projection can be created;
- no third canonical profile can be registered;
- no third operation or carrier policy can be admitted;
- no third live-smoke profile can be created;
- no third operator can enter canon-lock governance.

This is a valid null result.

## Conceptual repository probes

The inspection searched conceptual identities including:

- TER;
- AT;
- ND;
- PO;
- JO;
- LIG;
- LIGJ;
- MSHEF;
- MAGE;
- SHTU.

Repository occurrences do not establish reviewed evidence.

Examples of why occurrence counts cannot select an operator:

- AT is heavily contaminated by ordinary English text and code usage;
- SHTU has many internal doctrine, fixture, and test references;
- PO and JO currently belong to polarity and mediator-axis logic;
- TER appears in milestone and negative-contract references;
- several probes have zero or near-zero repository occurrences.

A proto-root, token, doctrine statement, fixture, test, or internal gloss is not
a reviewed production source row.

## Required authoritative-source intake

A future third-operator candidate must provide all of the following before
production design begins.

### Identity

- smallest functional embryo;
- isolated standalone form;
- stable language identity;
- bounded standalone gloss.

### External source authority

- non-synthetic source;
- source title;
- author or editor;
- publisher or host;
- date or version;
- exact URL, archive reference, DOI, page, example, or entry locator;
- attested form;
- attested gloss;
- relevant grammar note;
- review identity and date;
- stable source or archive hash where available.

### Functional bridge

- explicit semantic bridge for bounded functional motivation;
- no historical-origin inference;
- no historical-transmission inference;
- no borrowing-direction inference;
- no winner inference;
- no ownership inference;
- no language-superiority inference;
- no candidate-truth inference.

### Runtime proof design

- direct positive proof word;
- bounded larger-word proof where applicable;
- transformed-operation proof where applicable;
- cross-operator negative controls;
- unrelated-input negative controls;
- carrier-isolation controls;
- complete citation-leak controls;
- partial citation-leak controls.

### Architecture

The future candidate must use the existing reusable owners:

1. reviewed source-row registry;
2. evidence-gate validator;
3. functional readiness;
4. machine authorization;
5. explicit production membership;
6. runtime projection;
7. canonical profile;
8. operation policy;
9. carrier policy;
10. profile-backed live smoke;
11. canon-lock admission.

It must not require:

- a bespoke RootMap branch;
- a bespoke API branch;
- a bespoke UI branch;
- a duplicate source registry;
- a duplicate citation registry.

## Allowed future outcomes

An authoritative-source investigation may conclude:

- `READY_FOR_SOURCE_ROW_DESIGN`;
- `NEEDS_AUTHORITATIVE_SOURCE`;
- `NEEDS_ISOLATED_FORM_PROOF`;
- `NEEDS_SEMANTIC_BRIDGE`;
- `NEEDS_OPERATION_POLICY`;
- `NEEDS_CARRIER_POLICY`;
- `NO_CURRENT_REVIEWED_THIRD_OPERATOR`;
- `NULL`.

A null result remains acceptable.

## Scope

This decision lane adds only:

- this report;
- one contract test.

It does not modify:

- source rows;
- runtime authorization;
- production membership;
- runtime projection;
- canonical profiles;
- operation policy;
- carrier policy;
- RootMap;
- API behavior;
- UI behavior;
- live-smoke execution;
- canon-lock admission.

No candidate is selected, promoted, or implemented.
