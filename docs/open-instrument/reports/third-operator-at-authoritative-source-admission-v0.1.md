# Third-Operator AT Authoritative-Source Admission v0.1

Status: RUNTIME_VERIFIED_NOT_CANON_LOCKED.

Date: 2026-08-16.

Project lane:

- Open Instrument / ZË-RO;
- `OPEN_INSTRUMENT_SIMPLE_FUNCTIONAL_MOTIVATION_V1`;
- Father/AT final-control repair.

## Superseded null decision

The earlier decision:

- `NO_CURRENT_REVIEWED_THIRD_OPERATOR`

was correct for the evidence available at that time.

It is now superseded for current runtime state because an authoritative external
lexical source has been reviewed for Albanian `at` = `father`.

The earlier report remains historical evidence and is not rewritten as though
the source had existed earlier.

## Reviewed lexical identity

Operator:

- `AT`.

Language:

- Albanian (`sq`).

Smallest bounded embryo:

- `AT`.

Reviewed isolated form:

- `at`.

Reviewed lexical gloss:

- `father`.

Related carrier already present in the proto-root library:

- `atë` = `father`.

## External authority

Source:

- Bardhyl Demiraj, *Albanische Etymologien
  (Untersuchungen zum albanischen Erbwortschatz)*, 1997;
- database form: *The Albanian inherited lexicon*;
- IEED / `https://ieed.ullet.net/alb.html`.

Exact locator used for bounded lexical review:

- `at [m] (tg) {2} 'father'`;
- `Alb. atë [m] (tg) 'father' (AE 83)`.

The source explains `(tg)` as both Tosk and Gheg.

This source is used only for the bounded lexical statement `at = father`.

No historical-origin or borrowing-direction inference is authorized by this
runtime admission.

## Homograph boundary

Bare modern `at` can participate in a separate horse/steed homograph lane.

Therefore:

- the carrier `at` is available to deterministic matching;
- reviewed functional AT evidence is target-authorized only through explicit
  canonical-profile positive proof;
- `father` is a positive proof word;
- bare input `at` is an explicit negative control for reviewed father-function
  projection;
- no global `at => father` winner rule is introduced.

## Runtime admission

AT uses the existing reusable owners:

1. reviewed source-row registry;
2. evidence-gate validator;
3. functional readiness;
4. machine authorization;
5. explicit production membership;
6. runtime projection;
7. canonical operator profile;
8. reviewed operation/carrier policy;
9. canonical discovery;
10. trusted RootMap reviewed projection;
11. profile-backed live smoke;
12. canon-lock admission evaluation.

No bespoke:

- Father API branch;
- Father RootMap branch;
- Father UI branch;
- duplicate source registry;
- hardcoded UI candidate

is authorized.

## Lifecycle

AT enters current runtime governance as:

- `reviewed_functional`;
- `runtime_verified`.

AT does **not** enter `canon_locked` in this repair.

Existing canon-lock admission remains:

- DA;
- DI.

A separate explicit canon-lock transition would be required before AT could
become `canon_locked`.

## Runtime-verified discovery boundary

AT enters this repair as `runtime_verified`, not `canon_locked`.

Therefore its deterministic discovery scope is intentionally narrower than
the existing canon-locked DA and DI operators:

- `canon_locked` profiles may continue broad structural discovery;
- for a `runtime_verified` profile, the newly reviewed isolated carrier is
  target-bounded by the explicit positive-proof and negative-control corpus;
- AT positive proof is `father`;
- bare `at` remains an explicit negative-control/homograph target;
- the pre-existing AT carriers `atë`, `ati`, and `pater` remain available to
  generic structural DeepRoot discovery and do not automatically gain reviewed
  functional-evidence status;
- unrelated words such as `diet` or `data` must not acquire AT through generic
  transformations into the newly admitted `at` carrier;
- generic vowel-swap matching must not manufacture reviewed-isolated `at`
  outside that bounded target corpus.

This lifecycle rule is generic and profile-driven. It does not introduce a
Father-specific runtime branch, and it does not remove previously supported
structural AT carriers.

## Product target

Normal `father` analysis must expose the reviewed functional candidate before
legacy surface/seed records:

- Language: Albanian;
- Candidate: `AT`;
- meaning: `father`;
- evidence: Reviewed;
- surface path remains `A -> E`;
- functional path remains `A -> Ë`;
- delta remains `DIVERGE`.

Legacy `pater` / `atë` surface-resonance seed records may remain secondary
diagnostic/context records.

## Claim boundaries

This admission does not claim:

- historical origin;
- historical transmission;
- borrowing direction;
- winner;
- language superiority;
- linguistic ownership;
- publication evidence;
- scientific evidence;
- unrestricted candidate truth.

The user decides.

## Milestone posture

The parent milestone remains:

- `ACTIVE`.

It may not close until this AT repair passes focused tests, the full gate, CI,
review, browser proof, merge, final merged-main proof, and explicit user
approval.
