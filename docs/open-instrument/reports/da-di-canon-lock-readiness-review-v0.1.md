# Open Instrument — DA and DI Canon-Lock Readiness Review v0.1

## Status

READ-ONLY REVIEW.

No lifecycle mutation is authorized by this review.

Current lifecycle state:

- DA: `runtime_verified`;
- DI: `runtime_verified`.

Neither operator is changed to:

- `canon_locked`.

## Reviewed base

Open Instrument main:

- short SHA: `9fde11ef`;
- PR #1728 profile-backed live smoke merged and post-merge verified.

## Review standard

This review applies:

- `docs/open-instrument/canonical-operator-canon-lock-admission-contract-v0.1.md`.

The review distinguishes:

- engineering and runtime readiness;
- canon-lock governance readiness;
- stronger historical-source authority.

## Shared engineering readiness

DA and DI both currently prove:

- reviewed functional evidence;
- exact canonical profile ownership;
- functional lexical readiness;
- machine-readable runtime authorization;
- explicit production membership;
- non-null runtime projection;
- non-empty reviewed evidence text;
- positive proof words;
- negative control words;
- profile-backed production live smoke;
- cross-operator citation isolation;
- preserved claim boundaries;
- full repository gate stability.

Therefore both operators correctly remain:

- `runtime_verified`.

## DA readiness assessment

### Current admitted scope candidate

Potential scope:

- `bounded_functional_lexical_projection`.

### Evidence strength

DA has:

- reviewed isolated Gheg `da`;
- bounded function:
  - split;
  - divide;
  - cut;
  - separate;
- academic citation identity;
- DOI:
  - `10.3765/plsa.v8i1.5501`;
- explicit locator:
  - Example (4), page 3, footnote 1.

This source authority is sufficient for reviewing DA under bounded functional
lexical projection.

It does not establish historical origin.

### Functional stability

DA has explicit controls for:

- isolated `da`;
- bounded DA-family expansion;
- derivative evidence;
- homophone evidence;
- unrelated input;
- cross-operator evidence.

NDAJ and NDARË remain derivative support.

They do not replace isolated DA evidence.

The `da = gave` homophone does not validate `da = split/divide`.

### Runtime stability

DA currently has positive proof words:

- `da`;
- `dam`;
- `damage`.

DA negative controls:

- `study`;
- `xyz`.

Production live smoke confirms expected DA evidence visibility and absence.

### Reusable-process proof

DA has completed:

1. reviewed source row;
2. promotion and evidence closure;
3. readiness;
4. machine authorization;
5. production membership;
6. runtime projection;
7. RootMap visibility;
8. canonical profile;
9. shared canonical assertions;
10. profile-backed live smoke.

### DA governance finding

DA satisfies the substantive evidence, boundary, runtime, coverage, and
reusable-process requirements for a later scope-specific canon-lock decision.

However, this review is not a lifecycle mutation PR.

DA remains:

- `runtime_verified`.

A separate lifecycle-transition PR is required before DA can become
`canon_locked`.

## DI readiness assessment

### Current admitted scope candidate

Potential scope:

- `bounded_functional_lexical_projection`.

### Evidence strength

DI has reviewed functional evidence for:

- isolated Albanian `di`;
- bounded function:
  - know;
  - knowledge.

Current source identity includes the reviewed Wiktionary page and cited lexical
references associated with that entry.

Direct DPEWA/FGJSH authority remains unresolved for stronger source-authority
or historical-authority claims.

That unresolved stronger-source limitation does not invalidate DI's current
bounded runtime projection.

It does restrict stronger canon framing.

### Functional stability

DI has:

- stable embryo:
  - `DI`;
- bounded lexical function:
  - know / knowledge;
- positive runtime proof through:
  - `study`;
- negative controls:
  - `da`;
  - `dam`;
  - `damage`;
  - `mode`;
  - `xyz`;
- explicit DA citation exclusion;
- unrelated-input exclusion;
- preserved candidate-truth boundary.

DI is not declared the historical origin of `study`.

### Runtime stability

DI passes:

- readiness;
- machine authorization;
- production membership;
- runtime projection;
- canonical profile resolution;
- shared canonical assertions;
- profile-backed production live smoke.

### Coverage limitation

DI currently has one positive proof word:

- `study`.

That is sufficient for its current bounded runtime contract.

It is not yet enough, by itself, to establish a broadly reusable canon reference
with the same breadth as DA.

Before a later DI canon-lock mutation, the transition review should justify one
of the following:

- why one positive proof word is sufficient for the exact admitted scope; or
- add further independently reviewed positive proof coverage.

### Source-authority limitation

DI's unresolved direct DPEWA/FGJSH capture remains relevant when considering
canon lock because canon lock creates a stronger reusable-reference posture
than ordinary runtime eligibility.

The current evidence supports:

- bounded functional runtime use.

The current review does not establish:

- unrestricted historical authority;
- earliest origin;
- historical transmission;
- stronger dictionary-source closure.

### DI governance finding

DI remains correctly:

- `runtime_verified`.

DI is not yet recommended for a lifecycle mutation to `canon_locked`.

The blockers for a later DI canon-lock transition are:

1. explicit justification of source authority for the admitted canon-lock
   scope;
2. explicit treatment of unresolved direct DPEWA/FGJSH authority;
3. positive-proof coverage justification or expansion;
4. operator-specific canon-lock transition review.

## Comparative decision

### DA

Finding:

- substantively eligible for a later scope-specific canon-lock transition;
- no lifecycle mutation in this review.

Current state:

- `runtime_verified`.

### DI

Finding:

- engineering-ready and runtime-stable;
- not yet recommended for canon-lock transition.

Current state:

- `runtime_verified`.

## Claim boundaries

For both DA and DI:

- historical origin is not claimed;
- historical transmission is not claimed;
- winner status is not claimed;
- language superiority is not claimed;
- candidate truth is not claimed;
- user decision posture remains `user_decides`.

## Next safe sequence

1. review and merge this canon-lock admission definition lane;
2. open a separate DA-only lifecycle-transition lane;
3. preserve DI as `runtime_verified`;
4. review DI source-authority and positive-proof coverage separately;
5. after the first canon lock is stable, add the strongest-evidence third
   operator through the same reusable framework.

## Scope boundary

This review does not change:

- canonical profiles;
- lifecycle values;
- source rows;
- citations;
- readiness;
- authorization;
- production membership;
- runtime projection;
- RootMap;
- API;
- UI;
- analysis behavior;
- proof words;
- negative controls;
- live-smoke behavior.
