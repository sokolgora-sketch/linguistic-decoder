# Open Instrument — DA Canon-Lock Transition Review v0.1

## Decision

DA is accepted as:

- `canon_locked`

under:

- `bounded_functional_lexical_projection`.

DI remains:

- `runtime_verified`.

## DA admission evidence

DA preserves:

- reviewed isolated Gheg `da`;
- bounded split/divide functional motivation;
- source ID:
  `reviewed.external.gheg-da.damage.candidate.v0_1`;
- Dedvukaj and Ndoci 2023 PLSA citation identity;
- DOI:
  `10.3765/plsa.v8i1.5501`;
- locator:
  Example (4), page 3, footnote 1;
- derivative boundary protection;
- homophone boundary protection;
- deterministic runtime projection;
- positive proof words:
  - `da`;
  - `dam`;
  - `damage`;
- negative controls:
  - `study`;
  - `xyz`;
- cross-operator citation isolation;
- profile-backed production live smoke;
- all not-claimed boundaries.

## Scope decision

The existing `authorizationScope` is used as the admitted canon-lock scope.

A second profile field would duplicate machine-readable scope truth and is not
introduced.

## Machine-readable admission decision

The admission evaluator explicitly admits DA and fails closed when required
runtime, identity, proof, or claim-boundary conditions are absent.

DI is not included in the admitted operator set.

## Reversibility

DA may return to:

- `runtime_verified`

through a separate reviewed rollback PR.

A transition to:

- `deprecated`

must remain separate and explicit.

## No behavioral promotion

This transition changes lifecycle governance.

It does not change:

- evidence content;
- source authority;
- production membership;
- runtime projection;
- RootMap behavior;
- API behavior;
- UI behavior;
- candidate ranking;
- historical interpretation.

Canon lock means reusable Open Instrument implementation reference status only.
