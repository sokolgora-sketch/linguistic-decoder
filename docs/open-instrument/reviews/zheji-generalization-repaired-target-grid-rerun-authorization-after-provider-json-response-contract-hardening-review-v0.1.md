# Repaired target-grid rerun authorization after provider JSON response contract hardening review v0.1

Date: 2026-06-24

Status: REPAIRED_TARGET_GRID_RERUN_AUTHORIZATION_AFTER_PROVIDER_JSON_RESPONSE_CONTRACT_HARDENING_REVIEWED_ACCEPTED_READY_FOR_ONE_CONTROLLED_ENGLISH_COMIC_LOCAL_EXECUTION.

Reviewed authorization definition base:

* Short SHA: `10dcb6bd`
* Full SHA: `10dcb6bd3c3626b25e662d10ed7cd5da58f1a5bf`
* Subject: `docs(open-instrument): define repaired target-grid rerun authorization after provider JSON response contract hardening v0.1`

Reviewed authorization definition:

* `docs/open-instrument/zheji-generalization-repaired-target-grid-rerun-authorization-after-provider-json-response-contract-hardening-v0.1.md`

Authorized execution base for the future execution PR:

* `eb945b03d02eb93946dd3bd559325fd7536a5d82`

Artifact before future execution:

* `docs/open-instrument/artifacts/zheji-generalization/comic-layer2-target-grid-replay-v0.1.json`
* SHA-256: `5ce461b11f2e8d6811b1ef4d607b189c83ab040003e9a93ea927d12a91d3193a`

## Review verdict

The rerun authorization after provider JSON response contract hardening is accepted.

One future controlled local-only execution PR is allowed.

The future execution remains scoped to the exact English target `comic`.

The future execution must use the authorized implementation base:

* `eb945b03d02eb93946dd3bd559325fd7536a5d82`

The future execution must not use this review documentation commit as the execution base.

## Critical caveats accepted

The authorization correctly records the current source-language scope caveat.

The future clean `comic` rerun must not be cited as proof that source-language anti-tautology generalizes to non-English source targets.

Before any non-English source target, broad replay generalization, or source-language anti-tautology generalization claim, a separate reviewed PR must either:

* collapse the replay lane to explicit English-only scope
* or implement real reviewed source-language resolution

The authorization correctly records the `.test.ts` discovery caveat.

Before broader milestone closure or RootMap/selection-logic trust claims, a separate reviewed PR must either:

* rename undiscovered `.test.ts` files to `.spec.ts`
* or explicitly document intentional exclusion

These caveats do not block the exact English `comic` rerun.

These caveats do block broader generalization claims.

## Required future execution constraints

The future execution PR must prove before provider/model call:

* current checkout is exactly `eb945b03d02eb93946dd3bd559325fd7536a5d82`
* provider identity is local-only
* runner has `PROVIDER_JSON_RESPONSE_CONTRACT_HARDENING_V0_1`
* every request has `response_format.type == "json_object"`
* runner has the non-JSON fail-closed invalidation path
* runner has no automatic retry surface
* strict prompt-delivery proof still passes
* focused runner/prompt/scaffold tests pass
* target artifact path is exactly `docs/open-instrument/artifacts/zheji-generalization/comic-layer2-target-grid-replay-v0.1.json`

The future execution PR may mutate only the target artifact.

The future execution PR must not change runner code, tests, schemas, package files, CI, UI, runtime API, or documentation contracts.

## Provider boundary

The future execution must remain local-only.

No API key is allowed.

No Authorization header is allowed.

No Bearer token is allowed.

No remote hosted endpoint is allowed.

No DeepSeek endpoint is allowed.

No OpenAI hosted endpoint is allowed.

## Current PR scope

This PR is docs-only.

This PR does not:

* execute a replay
* call a provider
* call a model
* mutate an artifact
* change runner code
* change tests
* change schema
* change package files
* change CI
* promote evidence
* frame results for publication

## Claim boundary

This review is development-only.

It does not prove origin.

It does not prove functional motivation.

It does not prove any candidate true.

It does not crown a winner.

It does not create publication evidence.

It does not promote evidence.

It only accepts one future controlled local-only execution PR for the exact English `comic` rerun.

## Checks used

The review used:

* authorization definition proof
* critical caveat proof
* reviewed implementation source proof
* requestBody JSON-contract proof
* strict prompt proof
* runner self-check
* no automatic retry proof
* artifact unchanged SHA proof
* focused regression tests
* full Jest suite with longer timeout for known unrelated Evals timeout risk
* integration tests
* production build
* `git diff --check`

## Next accepted task

`test(open-instrument): execute reviewed repaired target-grid rerun after provider JSON response contract hardening v0.1`
