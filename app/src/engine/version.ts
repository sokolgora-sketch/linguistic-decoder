// app/src/engine/version.ts
// Keep this stable/deterministic for tests and snapshots.
// Bump intentionally when engine output shape/behaviour changes.

export const ENGINE_VERSION = "v1.0.0" as const;
